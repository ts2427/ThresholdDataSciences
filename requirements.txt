jinja2>=3.1
markdown>=3.5
pyyaml>=6.0
# Optional, only for local tooling:
#   pillow    — regenerate placeholder favicons/og-image (tools/make_placeholders.py)
#   selenium  — run the axe accessibility audit (tools/a11y_audit.py)
