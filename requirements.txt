Jinja2==3.1.6
Markdown==3.10.3
PyYAML==6.0.2
# Per-issue share-card generation (pure pip wheels; no system packages):
Pillow==11.1.0
fonttools==4.56.0
Brotli==1.2.0
# Optional, local tooling only (not needed for the build or the deploy):
#   selenium    — run the axe accessibility audit (tools/a11y_audit.py)
#   feedparser  — validate the RSS feed locally (tools/check_feed.py)
